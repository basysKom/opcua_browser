import QtQuick

Item {
    property color background: "transparent"
    property color backgroundSelected: "transparent"
    property color textColor: mediumLight
    property color textColorSelected: accent
}
